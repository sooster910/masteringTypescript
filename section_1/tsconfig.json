{
  "include": [
    "src" // src경로 내 모든 파일 한번에 컴파일
  ],

  "compilerOptions": {
    "target": "ESNext",
    // compile 된 js소스코드의 버전                                /* Set  the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    /* Modules */
    "module": "ESNext",
    // compile된 js 소스 코드에서 모듈 시스템 변환                                 /* Specify what module code is generated. *//* Specify a file that bundles all outputs into one JavaScript file. If 'declaration' is true, also designates a file that bundles all .d.ts output. */
    "outDir": "dist",
    // compile된 모든 소스 코드 위치한 디렉토리                                 /* Specify an output folder for all emitted files. */
    "moduleDetection": "force",
    // 각 파일을 독립된 모듈 스코프로 분리. export가 없는 파일도 모듈로 처리하여 변수 충돌 예방
    "strict": true,
  },

}
